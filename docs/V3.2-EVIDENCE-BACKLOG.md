# V3.2 Evidence Backlog — Findings from V3.1 Field Validation

**Generated:** 2026-09-11T21:30:04.169196

## Classification: P0 (Critical) - P4 (Enhancement)

### P1 — High Priority

| Finding | Evidence | Impact |
|---------|----------|--------|
| Mode discrimination must extend to ALL storage paths | Added mode field to 6 data structures, 6 store types needed separation | Without this, COLD/LEARNED comparison is invalid |
| DecisionImpactTracker does not load persisted impacts on init | `_impacts` is in-memory only; re-instantiation loses data | Decision influence metrics miss decisions across validation runs |
| outcome_improved is mostly None in real execution | Only populated via record_outcomes(), which was never wired in | Decision influence metrics are incomplete |
| Contamination verification required after every run | Single shared stores can leak LEARNED data into COLD | Invalidates the entire COLD vs LEARNED comparison |

### P2 — Medium Priority

| Finding | Evidence | Impact |
|---------|----------|--------|
| Leverage simulation is keyword-based (set overlap only) | MultiFactorRetriever._compute_scores uses word set overlap | Limits cross-task generalization detection |
| GeneralizationGain uses hard-coded 0.5 random baseline | evaluation.py line 145: `baseline_gen = 0.5` | Should use actual COLD baseline for accurate gain |
| Exploration in LEARNED mode uses random.seed-based rate | AdaptiveExplorationPolicy.should_explore uses random.random() | Non-reproducible exploration decisions |
| 10 warmup tasks insufficient for LEARNED strategy formation | Only 2 strategies generated after warmup; need min 3 experiences | Full learning requires more warmup exposures |
| Similar significance threshold hard-coded at 5pp | Evaluation.py: `significant = success_improvement >= 0.05` | Should be configurable per deployment |

### P3 — Low Priority

| Finding | Evidence | Impact |
|---------|----------|--------|
| PlanLearningStore searches all files — no mode partitioning | No COLD/LEARNED filter in plan outcome queries | Can mix plan outcomes across modes |
| PlanScorer weights hard-coded (0.30/0.25/0.20/0.15/0.10) | plan_intelligence.py — weights not configurable | Affects COLD vs LEARNED comparison fairness |
| OptimizationEngine._simulate_optimization assumes 20% improvement | optimization.py — hard-coded improvement assumption | Not useful for real measurement |
| FailureToLearningPipeline stores into V3 ExperienceStore but not V3.1 | Failure lessons skip V3.1 StructuredExperience pipeline | Loss of failure-derived learning |
| EvidenceStore chain_index.json grows unbounded | Append-only with no compaction mechanism | Disk usage concern at scale (not an issue for 260 execs) |

### P4 — Enhancement / Future

| Finding | Evidence | Notes |
|---------|----------|-------|
| No task difficulty/complexity field on experiences | Needed for stratified analysis by difficulty | Add difficulty tag to StructuredExperience |
| No semantic/embedding-based similarity | MultiFactorRetriever uses keyword overlap | Add embedding vector store for V3.2 |
| No validation_run_id or benchmark_id on experiences | Makes grouping across validation runs harder | Add run_id to all stored objects |
| Exploration is LEARNED-mode only — no ablation for exploration vs no-exploration | Can't measure exploration contribution separately | Add exploration ablation mode for V3.2 |
| ExperimentTracker file-based, not namespaced per validation run | model_intelligence.py — flat file store | Add run-namespaced tracking |
| 3x repetition may not be enough for statistical significance | CI for bootstrap confidence intervals | Consider 5-10x for high-variance tasks in V3.2 |

## Summary of Field Validation Findings

- **COLD total executions:** 130
- **LEARNED total executions:** 130
- **COLD success rate:** 48.5%
- **LEARNED success rate:** 81.5%
- **Success rate improvement:** 33.1pp
- **Score delta:** +0.1620
- **Strategies generated:** 122
- **Decisions influenced by learning:** 40
- **Contamination violations:** 0
- **P1 findings:** 4
- **P2 findings:** 5
- **P3 findings:** 5
- **P4 findings:** 6

---
*Backlog generated from V3.1 Field Validation execution.*